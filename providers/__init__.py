# One file per LLM provider. Each defines a class implementing
# llm_provider.LLMProvider. gemini_provider.py is the real, working one;
# openai_provider.py is an unverified reference implementation proving the
# same metric content can target a second provider -- see its docstring.
