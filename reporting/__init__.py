# Turns a pipeline run's raw result JSON into the human-facing report.
#
# report.py -- build_report() (result JSON -> report_contract.json's
#              shape) and render_html() (-> report.html).
#
# report_contract.json / report.html / report_preview_new_format.html /
# lomb_report_standalone.html live alongside report.py here as reference
# output, not as inputs anything imports.
