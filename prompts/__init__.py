# Makes prompts/ a package. One file per LLM-assisted metric lives here --
# each defines a single module-level constant named CONFIG
# (a metric_types.MetricPromptConfig). See registry.py for how these get
# wired together.
